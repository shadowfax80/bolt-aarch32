diff --git a/bolt/lib/Passes/LongJmp.cpp b/bolt/lib/Passes/LongJmp.cpp
index b771e6a8b120..c762ecd39ea1 100644
--- a/bolt/lib/Passes/LongJmp.cpp
+++ b/bolt/lib/Passes/LongJmp.cpp
@@ -11,6 +11,7 @@
 //===----------------------------------------------------------------------===//
 
 #include "bolt/Passes/LongJmp.h"
+#include "llvm/ADT/ScopeExit.h"
 #include "bolt/Core/ParallelUtilities.h"
 #include "bolt/Utils/CommandLineOpts.h"
 #include "llvm/Support/MathExtras.h"
@@ -24,6 +25,8 @@ extern cl::OptionCategory BoltCategory;
 extern cl::OptionCategory BoltOptCategory;
 extern cl::opt<bool> UseOldText;
 extern cl::opt<bool> HotFunctionsAtEnd;
+extern size_t padFunctionBefore(const bolt::BinaryFunction &Function);
+extern size_t padFunctionAfter(const bolt::BinaryFunction &Function);
 
 static cl::opt<bool> GroupStubs("group-stubs",
                                 cl::desc("share stubs across functions"),
@@ -323,7 +326,7 @@ uint64_t LongJmpPass::tentativeLayoutRelocColdPart(
       continue;
     DotAddress = alignTo(DotAddress, Func->getMinAlignment());
     uint64_t Pad =
-        offsetToAlignment(DotAddress, llvm::Align(Func->getAlignment()));
+        offsetToAlignment(DotAddress, llvm::Align(std::max<uint64_t>(Func->getAlignment(), 1)));
     if (Pad <= Func->getMaxColdAlignmentBytes())
       DotAddress += Pad;
     ColdAddresses[Func] = DotAddress;
@@ -390,9 +393,10 @@ LongJmpPass::tentativeLayoutRelocMode(const BinaryContext &BC,
 
     DotAddress = alignTo(DotAddress, Func->getMinAlignment());
     uint64_t Pad =
-        offsetToAlignment(DotAddress, llvm::Align(Func->getAlignment()));
+        offsetToAlignment(DotAddress, llvm::Align(std::max<uint64_t>(Func->getAlignment(), 1)));
     if (Pad <= Func->getMaxAlignmentBytes())
       DotAddress += Pad;
+    DotAddress += opts::padFunctionBefore(*Func);
     HotAddresses[Func] = DotAddress;
     LLVM_DEBUG(dbgs() << Func->getPrintName() << " tentative: "
                       << Twine::utohexstr(DotAddress) << "\n");
@@ -400,6 +404,7 @@ LongJmpPass::tentativeLayoutRelocMode(const BinaryContext &BC,
       DotAddress += Func->estimateSize();
     else
       DotAddress += Func->estimateHotSize();
+    DotAddress += opts::padFunctionAfter(*Func);
 
     if (uint64_t IslandSize = Func->estimateConstantIslandSize()) {
       DotAddress = alignTo(DotAddress, Func->getConstantIslandAlignment());
@@ -444,7 +449,7 @@ void LongJmpPass::tentativeLayout(const BinaryContext &BC,
     // Initial padding
     if (EstimatedTextSize <= BC.OldTextSectionSize) {
       DotAddress = BC.OldTextSectionAddress;
-      uint64_t Pad = offsetToAlignment(DotAddress, llvm::Align(BC.AlignText));
+      uint64_t Pad = offsetToAlignment(DotAddress, llvm::Align(std::max<uint64_t>(BC.AlignText, 1)));
       if (Pad + EstimatedTextSize <= BC.OldTextSectionSize) {
         DotAddress += Pad;
       }
@@ -531,9 +536,9 @@ Error LongJmpPass::relaxStub(BinaryBasicBlock &StubBB, bool &Modified) {
     return Error::success();
   }
 
-  // The long jmp uses absolute address on AArch64
-  // So we could not use it for PIC binaries
-  if (BC.isAArch64() && !BC.HasFixedLoadAddress)
+  // Absolute long/short veneers (AArch64 ADRP/ADD or ARM movw/movt) need a
+  // fixed load address, so reject PIC.
+  if ((BC.isAArch64() || BC.isARM()) && !BC.HasFixedLoadAddress)
     return createFatalBOLTError(
         "BOLT-ERROR: Unable to relax stub for PIC binary\n");
 
@@ -551,7 +556,8 @@ bool LongJmpPass::needsStub(const BinaryBasicBlock &BB, const MCInst &Inst,
   const BinaryFunction &Func = *BB.getFunction();
   const BinaryContext &BC = Func.getBinaryContext();
   const MCSymbol *TgtSym = BC.MIB->getTargetSymbol(Inst);
-  assert(TgtSym && "getTargetSymbol failed");
+  if (!TgtSym)
+    return false;
 
   const BinaryBasicBlock *TgtBB = Func.getBasicBlockForLabel(TgtSym);
   // Check for shared stubs from foreign functions
@@ -576,8 +582,12 @@ bool LongJmpPass::needsStub(const BinaryBasicBlock &BB, const MCInst &Inst,
 Error LongJmpPass::relax(BinaryFunction &Func, bool &Modified) {
   const BinaryContext &BC = Func.getBinaryContext();
 
-  assert(BC.isAArch64() && "Unsupported arch");
-  constexpr int InsnSize = 4; // AArch64
+  assert((BC.isAArch64() || BC.isARM()) && "Unsupported arch");
+  const MCSubtargetInfo *SavedSTI = BC.MIB->getSTI();
+  if (Func.isARMThumb() && BC.ThumbSTI)
+    BC.MIB->setSTI(BC.ThumbSTI.get());
+  auto RestoreSTI = llvm::make_scope_exit([&]() { BC.MIB->setSTI(SavedSTI); });
+  constexpr int InsnSize = 4; // AArch64 / ARM (Thumb-2 branches also 4)
   std::vector<std::pair<BinaryBasicBlock *, std::unique_ptr<BinaryBasicBlock>>>
       Insertions;
 
diff --git a/bolt/lib/Passes/VeneerElimination.cpp b/bolt/lib/Passes/VeneerElimination.cpp
index 9037390d06a4..60f7ad4ba001 100644
--- a/bolt/lib/Passes/VeneerElimination.cpp
+++ b/bolt/lib/Passes/VeneerElimination.cpp
@@ -36,7 +36,7 @@ namespace llvm {
 namespace bolt {
 
 Error VeneerElimination::runOnFunctions(BinaryContext &BC) {
-  if (!opts::EliminateVeneers || !BC.isAArch64())
+  if (!opts::EliminateVeneers || !(BC.isAArch64() || BC.isARM()))
     return Error::success();
 
   std::unordered_map<const MCSymbol *, const MCSymbol *> VeneerDestinations;
diff --git a/bolt/lib/Rewrite/BinaryPassManager.cpp b/bolt/lib/Rewrite/BinaryPassManager.cpp
index 6e3022c491a7..df13ae16f66e 100644
--- a/bolt/lib/Rewrite/BinaryPassManager.cpp
+++ b/bolt/lib/Rewrite/BinaryPassManager.cpp
@@ -384,7 +384,9 @@ Error BinaryFunctionPassManager::runAllPasses(BinaryContext &BC) {
 
   if (BC.isAArch64()) {
     Manager.registerPass(std::make_unique<FixRelaxations>(PrintFixRelaxations));
+  }
 
+  if (BC.isAArch64() || BC.isARM()) {
     Manager.registerPass(
         std::make_unique<VeneerElimination>(PrintVeneerElimination));
   }
@@ -537,15 +539,16 @@ Error BinaryFunctionPassManager::runAllPasses(BinaryContext &BC) {
     Manager.registerPass(
         std::make_unique<AArch64RelaxationPass>(PrintAArch64Relaxation));
 
-    // Tighten branches according to offset differences between branch and
-    // targets. No extra instructions after this pass, otherwise we may have
-    // relocations out of range and crash during linking.
-    Manager.registerPass(std::make_unique<LongJmpPass>(PrintLongJmp));
-
     Manager.registerPass(
         std::make_unique<PointerAuthCFIFixup>(PrintPAuthCFIFixup));
   }
 
+  // Tighten branches according to offset differences between branch and
+  // targets. Shared by AArch64 and AArch32 (ARM-mode veneers / LongJmp).
+  if (BC.isAArch64() || BC.isARM()) {
+    Manager.registerPass(std::make_unique<LongJmpPass>(PrintLongJmp));
+  }
+
   // This pass should always run last.*
   Manager.registerPass(std::make_unique<FinalizeFunctions>(PrintFinalized));
 
