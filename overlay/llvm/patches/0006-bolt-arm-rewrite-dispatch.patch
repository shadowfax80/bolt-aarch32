diff --git a/bolt/include/bolt/Rewrite/RewriteInstance.h b/bolt/include/bolt/Rewrite/RewriteInstance.h
index 2e63ca93c3e5..e427a32ffda4 100644
--- a/bolt/include/bolt/Rewrite/RewriteInstance.h
+++ b/bolt/include/bolt/Rewrite/RewriteInstance.h
@@ -321,6 +321,7 @@ private:
 
   /// Disassemble riscv-specific .plt \p Section auxiliary function
   void disassemblePLTSectionRISCV(BinarySection &Section);
+  void disassemblePLTSectionARM(BinarySection &Section);
 
   /// Update loadable segment information based on new sections.
   void updateSegmentInfo();
diff --git a/bolt/lib/Rewrite/RewriteInstance.cpp b/bolt/lib/Rewrite/RewriteInstance.cpp
index a12b83859669..695b640335df 100644
--- a/bolt/lib/Rewrite/RewriteInstance.cpp
+++ b/bolt/lib/Rewrite/RewriteInstance.cpp
@@ -370,6 +370,11 @@ MCPlusBuilder *createMCPlusBuilder(const Triple::ArchType Arch,
     return createRISCVMCPlusBuilder(Analysis, Info, RegInfo, STI);
 #endif
 
+#ifdef ARM_AVAILABLE
+  if (Arch == Triple::arm)
+    return createARMMCPlusBuilder(Analysis, Info, RegInfo, STI);
+#endif
+
   llvm_unreachable("architecture unsupported by MCPlusBuilder");
 }
 
@@ -1028,7 +1033,12 @@ void RewriteInstance::discoverFileObjects() {
       LastSymbol == SortedSymbols.end() ? LastSymbol : std::next(LastSymbol);
   for (auto Iter = SortedSymbols.begin(); Iter != SortedSymbolsEnd; ++Iter) {
     const SymbolRef &Symbol = Iter->Symbol;
-    const uint64_t SymbolAddress = Iter->Address;
+    uint64_t SymbolAddress = Iter->Address;
+    bool IsARMThumb = false;
+    if (BC->isARM()) {
+      IsARMThumb = SymbolAddress & 1;
+      SymbolAddress &= ~1ULL;
+    }
     const auto SymbolFlags = cantFail(Symbol.getFlags());
     const SymbolRef::Type SymbolType = cantFail(Symbol.getType());
 
@@ -1307,6 +1317,8 @@ void RewriteInstance::discoverFileObjects() {
 
       BF = BC->createBinaryFunction(UniqueName, *Section, SymbolAddress,
                                     SymbolSize);
+      if (IsARMThumb)
+        BF->setARMThumb(true);
       if (!IsSimple)
         BF->setSimple(false);
     }
@@ -2042,6 +2054,12 @@ void RewriteInstance::disassemblePLTSectionX86(BinarySection &Section,
   }
 }
 
+
+void RewriteInstance::disassemblePLTSectionARM(BinarySection &Section) {
+  (void)Section;
+  // Shared library PLT is out of scope for the initial ARM backend.
+}
+
 void RewriteInstance::disassemblePLT() {
   auto analyzeOnePLTSection = [&](BinarySection &Section, uint64_t EntrySize) {
     if (BC->isAArch64())
