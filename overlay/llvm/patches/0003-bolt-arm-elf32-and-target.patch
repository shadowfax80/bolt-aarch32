diff --git a/bolt/CMakeLists.txt b/bolt/CMakeLists.txt
index 8deb5cea0401..65b744b52f16 100644
--- a/bolt/CMakeLists.txt
+++ b/bolt/CMakeLists.txt
@@ -69,7 +69,7 @@ endif() # standalone
 
 # Determine default set of targets to build -- the intersection of
 # those BOLT supports and those LLVM is targeting.
-set(BOLT_TARGETS_TO_BUILD_all "AArch64;X86;RISCV")
+set(BOLT_TARGETS_TO_BUILD_all "AArch64;ARM;X86;RISCV")
 set(BOLT_TARGETS_TO_BUILD_default)
 foreach (tgt ${BOLT_TARGETS_TO_BUILD_all})
   if (tgt IN_LIST LLVM_TARGETS_TO_BUILD)
diff --git a/bolt/include/bolt/Core/BinaryContext.h b/bolt/include/bolt/Core/BinaryContext.h
index 73ccb911039d..3b0d803e503e 100644
--- a/bolt/include/bolt/Core/BinaryContext.h
+++ b/bolt/include/bolt/Core/BinaryContext.h
@@ -651,6 +651,7 @@ public:
   unsigned RegularPageSize{0x1000};
   static constexpr unsigned RegularPageSizeX86 = 0x1000;
   static constexpr unsigned RegularPageSizeAArch64 = 0x10000;
+  static constexpr unsigned RegularPageSizeARM = 0x1000;
 
   /// Huge page size to use.
   static constexpr unsigned HugePageSize = 0x200000;
@@ -706,6 +707,11 @@ public:
   /// Symbolic disassembler.
   std::unique_ptr<MCDisassembler> SymbolicDisAsm;
 
+  /// Thumb-mode disassembler for AArch32 functions with LSB=1.
+  std::unique_ptr<const MCSubtargetInfo> ThumbSTI;
+  std::unique_ptr<MCDisassembler> ThumbDisAsm;
+  std::unique_ptr<MCDisassembler> ThumbSymbolicDisAsm;
+
   std::unique_ptr<MCAsmBackend> MAB;
 
   /// Allows BOLT to print to log whenever it is necessary (with or without
@@ -941,6 +947,8 @@ public:
 
   bool isRISCV() const { return TheTriple->isRISCV(); }
 
+  bool isARM() const { return TheTriple->getArch() == llvm::Triple::arm; }
+
   // AArch64/RISC-V functions to check if symbol is used to delimit
   // code/data in .text. Code is marked by $x, data by $d.
   MarkerSymType getMarkerType(const SymbolRef &Symbol) const;
diff --git a/bolt/include/bolt/Core/BinaryFunction.h b/bolt/include/bolt/Core/BinaryFunction.h
index b3af846c4eb5..9e07a13f098e 100644
--- a/bolt/include/bolt/Core/BinaryFunction.h
+++ b/bolt/include/bolt/Core/BinaryFunction.h
@@ -315,6 +315,9 @@ private:
   /// be not fully disassembled and will not be emitted.
   bool IsIgnored{false};
 
+  /// True if this function is Thumb (AArch32 symbol LSB set).
+  bool IsARMThumb{false};
+
   /// Pseudo functions should not be disassembled or emitted.
   bool IsPseudo{false};
 
@@ -1411,6 +1414,11 @@ public:
   /// Return true if the function should be ignored for optimization purposes.
   bool isIgnored() const { return IsIgnored; }
 
+  bool isARMThumb() const { return IsARMThumb; }
+  void setARMThumb(bool V = true) { IsARMThumb = V; }
+
+  MCDisassembler &getDisassembler() const;
+
   /// Return true if the function should not be disassembled, emitted, or
   /// otherwise processed.
   bool isPseudo() const { return IsPseudo; }
diff --git a/bolt/lib/Core/AddressMap.cpp b/bolt/lib/Core/AddressMap.cpp
index f061fea49439..bf3fc4c36746 100644
--- a/bolt/lib/Core/AddressMap.cpp
+++ b/bolt/lib/Core/AddressMap.cpp
@@ -70,8 +70,12 @@ std::optional<AddressMap> AddressMap::parse(BinaryContext &BC) {
 
   AddressMap Parsed;
 
-  unsigned CodePointerSize = BC.AsmInfo->getCodePointerSize();
-  const size_t EntrySize = 2 * CodePointerSize;
+  // emitAddress/emitLabel always write two 8-byte fields (host MCSymbol*
+  // pointer + output address), including on ELF32. Parsing with the target
+  // pointer size splits each record and treats the unresolved high half
+  // (often 0) as a second label — aborting ARM32 rewrite.
+  const unsigned FieldSize = 8;
+  const size_t EntrySize = 2 * FieldSize;
   auto parseSection =
       [&](BinarySection &Section,
           function_ref<void(uint64_t, uint64_t)> InsertCallback) {
@@ -82,8 +86,8 @@ std::optional<AddressMap> AddressMap::parse(BinaryContext &BC) {
         DataExtractor::Cursor Cursor(0);
 
         while (Cursor && !DE.eof(Cursor)) {
-          const uint64_t Input = DE.getUnsigned(Cursor, CodePointerSize);
-          const uint64_t Output = DE.getUnsigned(Cursor, CodePointerSize);
+          const uint64_t Input = DE.getUnsigned(Cursor, FieldSize);
+          const uint64_t Output = DE.getUnsigned(Cursor, FieldSize);
           InsertCallback(Input, Output);
         }
 
@@ -103,11 +107,9 @@ std::optional<AddressMap> AddressMap::parse(BinaryContext &BC) {
   if (LabelMapSection) {
     Parsed.Label2AddrMap.reserve(LabelMapSection->getOutputSize() / EntrySize);
     parseSection(*LabelMapSection, [&](uint64_t Input, uint64_t Output) {
-      assert(!Parsed.Label2AddrMap.count(
-                 reinterpret_cast<const MCSymbol *>(Input)) &&
-             "Duplicate label entry detected.");
-      Parsed.Label2AddrMap.insert(
-          {reinterpret_cast<const MCSymbol *>(Input), Output});
+      const auto *Sym = reinterpret_cast<const MCSymbol *>(Input);
+      if (!Parsed.Label2AddrMap.count(Sym))
+        Parsed.Label2AddrMap.insert({Sym, Output});
     });
   }
 
diff --git a/bolt/lib/Core/BinaryBasicBlock.cpp b/bolt/lib/Core/BinaryBasicBlock.cpp
index a6d0ca948115..f22e57921651 100644
--- a/bolt/lib/Core/BinaryBasicBlock.cpp
+++ b/bolt/lib/Core/BinaryBasicBlock.cpp
@@ -493,6 +493,11 @@ uint32_t BinaryBasicBlock::getNumPseudos() const {
     BC.errs() << "BOLT-ERROR: instructions for basic block " << getName()
               << " in function " << *Function << ": calculated pseudos " << N
               << ", set pseudos " << NumPseudos << ", size " << size() << '\n';
+    if (BC.isARM()) {
+      const_cast<BinaryBasicBlock *>(this)->NumPseudos = N;
+      Function->setIgnored();
+      return N;
+    }
     llvm_unreachable("pseudos mismatch");
   }
 #endif
diff --git a/bolt/lib/Core/BinaryContext.cpp b/bolt/lib/Core/BinaryContext.cpp
index 130c523bcda5..d43efdd84275 100644
--- a/bolt/lib/Core/BinaryContext.cpp
+++ b/bolt/lib/Core/BinaryContext.cpp
@@ -158,7 +158,12 @@ BinaryContext::BinaryContext(std::unique_ptr<MCContext> Ctx,
       STI(std::move(STI)), InstPrinter(std::move(InstPrinter)),
       MIA(std::move(MIA)), MIB(std::move(MIB)), MRI(std::move(MRI)),
       DisAsm(std::move(DisAsm)), Logger(Logger), InitialDynoStats(isAArch64()) {
-  RegularPageSize = isAArch64() ? RegularPageSizeAArch64 : RegularPageSizeX86;
+  if (isAArch64())
+    RegularPageSize = RegularPageSizeAArch64;
+  else if (isARM())
+    RegularPageSize = RegularPageSizeARM;
+  else
+    RegularPageSize = RegularPageSizeX86;
   PageAlign = opts::NoHugePages ? RegularPageSize : HugePageSize;
 }
 
@@ -207,6 +212,13 @@ Expected<std::unique_ptr<BinaryContext>> BinaryContext::createBinaryContext(
     FeaturesStr = Features->getString();
     break;
   }
+  case llvm::Triple::arm:
+    if (Features)
+      return createFatalBOLTError(
+          "ARM target does not use SubtargetFeatures");
+    ArchName = "arm";
+    FeaturesStr = "";
+    break;
   default:
     return createStringError(std::errc::not_supported,
                              "BOLT-ERROR: Unrecognized machine in ELF file");
@@ -221,6 +233,11 @@ Expected<std::unique_ptr<BinaryContext>> BinaryContext::createBinaryContext(
     return createStringError(make_error_code(std::errc::not_supported),
                              Twine("BOLT-ERROR: ", Error));
 
+  // JITLink ELF_aarch32 requires a parseable ARM arch (armv4/armv7/...), not
+  // the generic ELF "arm" name. Keep lookup on "arm" (the registered target).
+  if (TheTriple.getArch() == llvm::Triple::arm)
+    TheTriple.setArchName("armv7");
+
   std::unique_ptr<const MCRegisterInfo> MRI(
       TheTarget->createMCRegInfo(TheTriple));
   if (!MRI)
@@ -316,6 +333,17 @@ Expected<std::unique_ptr<BinaryContext>> BinaryContext::createBinaryContext(
   BC->SymbolicDisAsm = std::unique_ptr<MCDisassembler>(
       BC->TheTarget->createMCDisassembler(*BC->STI, *BC->Ctx));
 
+  if (TheTriple.getArch() == llvm::Triple::arm) {
+    BC->ThumbSTI.reset(BC->TheTarget->createMCSubtargetInfo(
+        TheTriple, "", "+thumb-mode"));
+    if (BC->ThumbSTI) {
+      BC->ThumbDisAsm = std::unique_ptr<MCDisassembler>(
+          BC->TheTarget->createMCDisassembler(*BC->ThumbSTI, *BC->Ctx));
+      BC->ThumbSymbolicDisAsm = std::unique_ptr<MCDisassembler>(
+          BC->TheTarget->createMCDisassembler(*BC->ThumbSTI, *BC->Ctx));
+    }
+  }
+
   BC->updateLSDAEncoding();
 
   if (!BC->SymbolicDisAsm)
@@ -1470,8 +1498,12 @@ void BinaryContext::processInterproceduralReferences() {
       continue;
     }
 
+    if (isARM())
+      Address &= ~1ULL;
+
     ErrorOr<BinarySection &> Section = getSectionForAddress(Address);
-    assert(Section && "cannot get section for referenced address");
+    if (!Section)
+      continue;
 
     if (!Section->isText())
       continue;
@@ -2069,6 +2101,13 @@ MarkerSymType BinaryContext::getMarkerType(unsigned SymbolType,
   if (isRISCV() && SymbolName.starts_with("$x"))
     return MarkerSymType::CODE;
 
+  if (isARM()) {
+    if (SymbolName == "$a" || SymbolName.starts_with("$a."))
+      return MarkerSymType::CODE;
+    if (SymbolName == "$t" || SymbolName.starts_with("$t."))
+      return MarkerSymType::CODE;
+  }
+
   if (SymbolName == "$d" || SymbolName.starts_with("$d."))
     return MarkerSymType::DATA;
 
diff --git a/bolt/lib/Core/BinaryEmitter.cpp b/bolt/lib/Core/BinaryEmitter.cpp
index a555c7185448..8222d818d045 100644
--- a/bolt/lib/Core/BinaryEmitter.cpp
+++ b/bolt/lib/Core/BinaryEmitter.cpp
@@ -20,6 +20,7 @@
 #include "bolt/Utils/Utils.h"
 #include "llvm/DebugInfo/DWARF/DWARFCompileUnit.h"
 #include "llvm/MC/MCSection.h"
+#include "llvm/ADT/ScopeExit.h"
 #include "llvm/MC/MCStreamer.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/LEB128.h"
@@ -73,7 +74,7 @@ size_t padFunction(std::map<std::string, size_t> &FunctionPadding,
       if (N == std::string::npos)
         continue;
       std::string Name = Spec.substr(0, N);
-      size_t Padding = std::stoull(Spec.substr(N + 1));
+      size_t Padding = std::stoull(Spec.substr(N + 1), nullptr, 0);
       FunctionPadding[Name] = Padding;
     }
   }
@@ -286,6 +287,11 @@ void BinaryEmitter::emitFunctions() {
 
 bool BinaryEmitter::emitFunction(BinaryFunction &Function,
                                  FunctionFragment &FF) {
+  const MCSubtargetInfo *SavedMIBSTI = BC.MIB->getSTI();
+  if (Function.isARMThumb() && BC.ThumbSTI)
+    BC.MIB->setSTI(BC.ThumbSTI.get());
+  auto RestoreMIBSTI = llvm::make_scope_exit([&]() { BC.MIB->setSTI(SavedMIBSTI); });
+
   if (Function.size() == 0 && !Function.hasIslandsInfo())
     return false;
 
@@ -505,7 +511,18 @@ void BinaryEmitter::emitFunctionBody(BinaryFunction &BF, FunctionFragment &FF,
         }
       }
 
-      Streamer.emitInstruction(Instr, *BC.STI);
+      // ARM LDM/STM encoders walk every trailing operand as a register.
+      // BOLT annotations are extra non-reg operands and abort CheckLargeFunctions
+      // / emit unless stripped first.
+      if (BC.isARM()) {
+        MCInst Clean = Instr;
+        BC.MIB->stripAnnotations(Clean);
+        const MCSubtargetInfo &EmitSTI =
+            (BF.isARMThumb() && BC.ThumbSTI) ? *BC.ThumbSTI : *BC.STI;
+        Streamer.emitInstruction(Clean, EmitSTI);
+      } else {
+        Streamer.emitInstruction(Instr, *BC.STI);
+      }
     }
   }
 
diff --git a/bolt/lib/Core/BinaryFunction.cpp b/bolt/lib/Core/BinaryFunction.cpp
index 31828aafb95f..3847a69d252a 100644
--- a/bolt/lib/Core/BinaryFunction.cpp
+++ b/bolt/lib/Core/BinaryFunction.cpp
@@ -35,6 +35,7 @@
 #include "llvm/Object/ObjectFile.h"
 #include "llvm/Support/CommandLine.h"
 #include "llvm/Support/Debug.h"
+#include "llvm/ADT/ScopeExit.h"
 #include "llvm/Support/GenericDomTreeConstruction.h"
 #include "llvm/Support/GenericLoopInfoImpl.h"
 #include "llvm/Support/GraphWriter.h"
@@ -167,6 +168,14 @@ bool shouldPrint(const BinaryFunction &Function) {
 
 namespace llvm {
 namespace bolt {
+MCDisassembler &BinaryFunction::getDisassembler() const {
+  if (BC.isARM() && BC.ThumbSymbolicDisAsm &&
+      (IsARMThumb || getOneName().starts_with("$t")))
+    return *BC.ThumbSymbolicDisAsm;
+  return *BC.SymbolicDisAsm;
+}
+
+
 
 template <typename R> static bool emptyRange(const R &Range) {
   return Range.begin() == Range.end();
@@ -1305,6 +1314,7 @@ BinaryFunction::getInstructionSequenceLength(uint64_t Offset,
 Error BinaryFunction::disassemble() {
   NamedRegionTimer T("disassemble", "Disassemble function", "buildfuncs",
                      "Build Binary Functions", opts::TimeBuild);
+
   ErrorOr<ArrayRef<uint8_t>> ErrorOrFunctionData = getData();
   assert(ErrorOrFunctionData && "function data is not available");
   ArrayRef<uint8_t> FunctionData = *ErrorOrFunctionData;
@@ -1314,7 +1324,13 @@ Error BinaryFunction::disassemble() {
   auto &Ctx = BC.Ctx;
   auto &MIB = BC.MIB;
 
-  BC.SymbolicDisAsm->setSymbolizer(MIB->createTargetSymbolizer(*this));
+  // Thumb helpers (createBranch, InThumbMode, sizes) key off MIB STI.
+  const MCSubtargetInfo *SavedSTI = MIB->getSTI();
+  if (BC.isARM() && isARMThumb() && BC.ThumbSTI)
+    MIB->setSTI(BC.ThumbSTI.get());
+  auto RestoreSTI = llvm::make_scope_exit([&]() { MIB->setSTI(SavedSTI); });
+
+  getDisassembler().setSymbolizer(MIB->createTargetSymbolizer(*this));
 
   // Insert a label at the beginning of the function. This will be our first
   // basic block.
@@ -1338,7 +1354,7 @@ Error BinaryFunction::disassemble() {
       continue;
     }
 
-    if (!BC.SymbolicDisAsm->getInstruction(Instruction, Size,
+    if (!getDisassembler().getInstruction(Instruction, Size,
                                            FunctionData.slice(Offset),
                                            AbsoluteInstrAddr, nulls())) {
       // Functions with "soft" boundaries, e.g. coming from assembly source,
@@ -1413,8 +1429,26 @@ Error BinaryFunction::disassemble() {
 
     if (MIB->isBranch(Instruction) || MIB->isCall(Instruction)) {
       uint64_t TargetAddress = 0;
-      if (MIB->evaluateBranch(Instruction, AbsoluteInstrAddr, Size,
-                              TargetAddress)) {
+      bool EvaluatedBranch = MIB->evaluateBranch(
+          Instruction, AbsoluteInstrAddr, Size, TargetAddress);
+      // ARM symbolizer replaces BL/B immediates with reloc symbols. The
+      // encoded displacement may be a linker veneer (OOR for MC) even though
+      // the reloc names the real callee — resolve the already-symbolized
+      // operand so reloc-mode disassembly stays non-fatal.
+      if (!EvaluatedBranch && BC.isARM()) {
+        if (const MCSymbol *TgtSym = MIB->getTargetSymbol(Instruction)) {
+          if (ErrorOr<uint64_t> Val = BC.getSymbolValue(*TgtSym)) {
+            TargetAddress = *Val;
+            EvaluatedBranch = true;
+          } else if (BinaryFunction *TgtBF = BC.getFunctionForSymbol(TgtSym)) {
+            TargetAddress = TgtBF->getAddress();
+            EvaluatedBranch = true;
+          }
+        }
+      }
+      if (EvaluatedBranch) {
+        if (BC.isARM())
+          TargetAddress &= ~1ULL;
         // Check if the target is within the same function. Otherwise it's
         // a call, possibly a tail call.
         //
@@ -1573,6 +1607,8 @@ add_instruction:
 
   // Reset symbolizer for the disassembler.
   BC.SymbolicDisAsm->setSymbolizer(nullptr);
+  if (BC.ThumbSymbolicDisAsm)
+    BC.ThumbSymbolicDisAsm->setSymbolizer(nullptr);
 
   if (uint64_t Offset = getFirstInstructionOffset())
     Labels[Offset] = BC.Ctx->createNamedTempSymbol();
@@ -1655,7 +1691,7 @@ bool BinaryFunction::scanExternalRefs() {
 
     const uint64_t AbsoluteInstrAddr = getAddress() + Offset;
     PrevInstruction = Instruction;
-    if (!BC.SymbolicDisAsm->getInstruction(Instruction, Size,
+    if (!getDisassembler().getInstruction(Instruction, Size,
                                            FunctionData.slice(Offset),
                                            AbsoluteInstrAddr, nulls())) {
       if (opts::Verbosity >= 1 && !isZeroPaddingAt(Offset)) {
@@ -3837,7 +3873,15 @@ void BinaryFunction::postProcessBranches() {
       }
     }
   }
-  assert(validateCFG() && "invalid CFG");
+  if (!validateCFG()) {
+    if (BC.isARM()) {
+      BC.errs() << "BOLT-WARNING: invalid CFG in " << *this
+                << " ??? marking as ignored\n";
+      setIgnored();
+      return;
+    }
+    assert(false && "invalid CFG");
+  }
 }
 
 MCSymbol *BinaryFunction::addEntryPointAtOffset(uint64_t Offset) {
@@ -4879,10 +4923,16 @@ bool BinaryFunction::isAArch64Veneer() const {
 }
 
 bool BinaryFunction::isPossibleVeneer() const {
-  return BC.isAArch64() &&
-         (isAArch64Veneer() || getOneName().starts_with("__AArch64") ||
-          getOneName().starts_with("e843419") ||
-          getOneName().starts_with("__CortexA53843419_"));
+  if (BC.isAArch64())
+    return isAArch64Veneer() || getOneName().starts_with("__AArch64") ||
+           getOneName().starts_with("e843419") ||
+           getOneName().starts_with("__CortexA53843419_");
+  // LLD ARM long-branch thunks, e.g. __ARMv5LongLdrPcThunk_<sym>
+  if (BC.isARM())
+    return getOneName().starts_with("__ARMv") ||
+           getOneName().starts_with("__Thumb") ||
+           getOneName().starts_with("__ARMV");
+  return false;
 }
 
 void BinaryFunction::addRelocation(uint64_t Address, MCSymbol *Symbol,
diff --git a/bolt/lib/Core/BinarySection.cpp b/bolt/lib/Core/BinarySection.cpp
index e803d17021f8..66f10e4ceb97 100644
--- a/bolt/lib/Core/BinarySection.cpp
+++ b/bolt/lib/Core/BinarySection.cpp
@@ -11,6 +11,8 @@
 //===----------------------------------------------------------------------===//
 
 #include "bolt/Core/BinarySection.h"
+#include "bolt/Core/BinaryFunction.h"
+#include "llvm/BinaryFormat/ELF.h"
 #include "bolt/Core/BinaryContext.h"
 #include "bolt/Utils/CommandLineOpts.h"
 #include "bolt/Utils/Utils.h"
@@ -183,6 +185,27 @@ void BinarySection::flushPendingRelocations(raw_pwrite_stream &OS,
     if (Reloc.Symbol)
       Value += Resolver(Reloc.Symbol);
 
+    // ARM/Thumb: absolute addresses of Thumb functions must have bit0 set so
+    // BX/BLX and function-pointer calls enter Thumb state.
+    if (Reloc.Symbol && BC.isARM()) {
+      switch (Reloc.Type) {
+      case ELF::R_ARM_ABS32:
+      case ELF::R_ARM_TARGET1:
+      case ELF::R_ARM_MOVW_ABS_NC:
+      case ELF::R_ARM_MOVT_ABS:
+      case ELF::R_ARM_THM_MOVW_ABS_NC:
+      case ELF::R_ARM_THM_MOVT_ABS: {
+        if (BinaryFunction *BF = BC.getFunctionForSymbol(Reloc.Symbol)) {
+          if (BF->isARMThumb())
+            Value |= 1ULL;
+        }
+        break;
+      }
+      default:
+        break;
+      }
+    }
+
     // Safely skip any optional pending relocation that cannot be encoded.
     if (Reloc.isOptional() &&
         !Relocation::canEncodeValue(Reloc.Type, Value,
