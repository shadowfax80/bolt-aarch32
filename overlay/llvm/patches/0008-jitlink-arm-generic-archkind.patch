diff --git a/llvm/include/llvm/ExecutionEngine/JITLink/aarch32.h b/llvm/include/llvm/ExecutionEngine/JITLink/aarch32.h
index 9479c107447d..c46d0afe11b5 100644
--- a/llvm/include/llvm/ExecutionEngine/JITLink/aarch32.h
+++ b/llvm/include/llvm/ExecutionEngine/JITLink/aarch32.h
@@ -209,7 +209,8 @@ struct FixupInfoArmBranch : public FixupInfoArm {
 };
 
 template <> struct FixupInfo<Arm_Jump24> : public FixupInfoArmBranch {
-  static constexpr uint32_t OpcodeMask = 0x0f000000;
+  // Bit 24 distinguishes B (0xA) from BL (0xB); both are R_ARM_JUMP24.
+  static constexpr uint32_t OpcodeMask = 0x0e000000;
 };
 
 template <> struct FixupInfo<Arm_Call> : public FixupInfoArmBranch {
diff --git a/llvm/lib/ExecutionEngine/JITLink/ELF_aarch32.cpp b/llvm/lib/ExecutionEngine/JITLink/ELF_aarch32.cpp
index f5ca72796df5..7d3f3ec426c8 100644
--- a/llvm/lib/ExecutionEngine/JITLink/ELF_aarch32.cpp
+++ b/llvm/lib/ExecutionEngine/JITLink/ELF_aarch32.cpp
@@ -260,6 +260,10 @@ Expected<std::unique_ptr<LinkGraph>> createLinkGraphFromELFObject_aarch32(
   // Find out what exact AArch32 instruction set and features we target.
   auto TT = (*ELFObj)->makeTriple();
   ARM::ArchKind AK = ARM::parseArch(TT.getArchName());
+  if (AK == ARM::ArchKind::INVALID &&
+      (TT.getArch() == Triple::arm || TT.getArch() == Triple::thumb ||
+       TT.getArch() == Triple::armeb || TT.getArch() == Triple::thumbeb))
+    AK = ARM::ArchKind::ARMV7A;
   if (AK == ARM::ArchKind::INVALID)
     return make_error<JITLinkError>(
         "Failed to build ELF link graph: Invalid ARM ArchKind");
@@ -301,6 +305,10 @@ void link_ELF_aarch32(std::unique_ptr<LinkGraph> G,
 
   using namespace ARMBuildAttrs;
   ARM::ArchKind AK = ARM::parseArch(TT.getArchName());
+  if (AK == ARM::ArchKind::INVALID &&
+      (TT.getArch() == Triple::arm || TT.getArch() == Triple::thumb ||
+       TT.getArch() == Triple::armeb || TT.getArch() == Triple::thumbeb))
+    AK = ARM::ArchKind::ARMV7A;
   auto CPU = static_cast<CPUArch>(ARM::getArchAttr(AK));
   aarch32::ArmConfig ArmCfg = aarch32::getArmConfigForCPUArch(CPU);
 
diff --git a/llvm/lib/ExecutionEngine/JITLink/aarch32.cpp b/llvm/lib/ExecutionEngine/JITLink/aarch32.cpp
index cc606b8278d2..5b956e0455dd 100644
--- a/llvm/lib/ExecutionEngine/JITLink/aarch32.cpp
+++ b/llvm/lib/ExecutionEngine/JITLink/aarch32.cpp
@@ -471,6 +471,8 @@ Error applyFixupData(LinkGraph &G, Block &B, const Edge &E) {
   }
   case Data_Pointer32: {
     int64_t Value = TargetAddress + Addend;
+    if (hasTargetFlags(TargetSymbol, ThumbSymbol))
+      Value |= 1;
     if (!isUInt<32>(Value))
       return makeTargetOutOfRangeError(G, B, E);
     if (LLVM_LIKELY(G.getEndianness() == endianness::little))
@@ -559,12 +561,18 @@ Error applyFixupArm(LinkGraph &G, Block &B, const Edge &E) {
     return Error::success();
   }
   case Arm_MovwAbsNC: {
-    uint16_t Value = (TargetAddress + Addend) & 0xffff;
+    uint64_t Abs = TargetAddress + Addend;
+    if (hasTargetFlags(TargetSymbol, ThumbSymbol))
+      Abs |= 1;
+    uint16_t Value = Abs & 0xffff;
     writeImmediate<Arm_MovwAbsNC>(R, encodeImmMovtA1MovwA2(Value));
     return Error::success();
   }
   case Arm_MovtAbs: {
-    uint16_t Value = ((TargetAddress + Addend) >> 16) & 0xffff;
+    uint64_t Abs = TargetAddress + Addend;
+    if (hasTargetFlags(TargetSymbol, ThumbSymbol))
+      Abs |= 1;
+    uint16_t Value = (Abs >> 16) & 0xffff;
     writeImmediate<Arm_MovtAbs>(R, encodeImmMovtA1MovwA2(Value));
     return Error::success();
   }
@@ -623,13 +631,20 @@ Error applyFixupThumb(LinkGraph &G, Block &B, const Edge &E,
         // aligned destination while instruction may only be 2-byte aligned
         R.Lo = R.Lo & ~FixupInfo<Thumb_Call>::LoBitNoBlx;
         R.Lo = R.Lo & ~FixupInfo<Thumb_Call>::LoBitH;
-        Value = alignTo(Value, 4);
       } else {
         // Change opcode BLX -> BL
-        R.Lo = R.Lo & ~FixupInfo<Thumb_Call>::LoBitNoBlx;
+        R.Lo = R.Lo | FixupInfo<Thumb_Call>::LoBitNoBlx;
       }
     }
 
+    // BLX T2 requires a 4-byte-aligned destination; Imm11[0]/H) must stay 0.
+    // Apply even when the instruction was already BLX (e.g. Thumb tBLXi).
+    InstrIsBlx = (R.Lo & FixupInfo<Thumb_Call>::LoBitNoBlx) == 0;
+    if (InstrIsBlx) {
+      R.Lo = R.Lo & ~FixupInfo<Thumb_Call>::LoBitH;
+      Value = alignTo(Value, 4);
+    }
+
     if (LLVM_LIKELY(ArmCfg.J1J2BranchEncoding)) {
       if (!isInt<25>(Value))
         return makeTargetOutOfRangeError(G, B, E);
@@ -640,6 +655,9 @@ Error applyFixupThumb(LinkGraph &G, Block &B, const Edge &E,
       writeImmediate<Thumb_Call>(R, encodeImmBT4BlT1BlxT2(Value));
     }
 
+    if (InstrIsBlx)
+      R.Lo = R.Lo & ~FixupInfo<Thumb_Call>::LoBitH;
+
     assert(((R.Lo & FixupInfo<Thumb_Call>::LoBitNoBlx) ||
             (R.Lo & FixupInfo<Thumb_Call>::LoBitH) == 0) &&
            "Opcode BLX implies H bit is clear (avoid UB in BLX T2)");
@@ -647,12 +665,18 @@ Error applyFixupThumb(LinkGraph &G, Block &B, const Edge &E,
   }
 
   case Thumb_MovwAbsNC: {
-    uint16_t Value = (TargetAddress + Addend) & 0xffff;
+    uint64_t Abs = TargetAddress + Addend;
+    if (hasTargetFlags(TargetSymbol, ThumbSymbol))
+      Abs |= 1;
+    uint16_t Value = Abs & 0xffff;
     writeImmediate<Thumb_MovwAbsNC>(R, encodeImmMovtT1MovwT3(Value));
     return Error::success();
   }
   case Thumb_MovtAbs: {
-    uint16_t Value = ((TargetAddress + Addend) >> 16) & 0xffff;
+    uint64_t Abs = TargetAddress + Addend;
+    if (hasTargetFlags(TargetSymbol, ThumbSymbol))
+      Abs |= 1;
+    uint16_t Value = (Abs >> 16) & 0xffff;
     writeImmediate<Thumb_MovtAbs>(R, encodeImmMovtT1MovwT3(Value));
     return Error::success();
   }
diff --git a/llvm/lib/Target/ARM/MCTargetDesc/ARMELFObjectWriter.cpp b/llvm/lib/Target/ARM/MCTargetDesc/ARMELFObjectWriter.cpp
index d914f6e830cf..67701f8d0e21 100644
--- a/llvm/lib/Target/ARM/MCTargetDesc/ARMELFObjectWriter.cpp
+++ b/llvm/lib/Target/ARM/MCTargetDesc/ARMELFObjectWriter.cpp
@@ -107,6 +107,12 @@ unsigned ARMELFObjectWriter::getRelocType(const MCFixup &Fixup,
   if (IsPCRel) {
     switch (Fixup.getKind()) {
     default:
+      // BOLT veneers use :lower16:/:upper16: on MOVi16/MOVTi16. The encoder
+      // may surface those specifiers with IsPCRel set; they are still ABS.
+      if (Specifier == ARM::S_LO16)
+        return ELF::R_ARM_MOVW_ABS_NC;
+      if (Specifier == ARM::S_HI16)
+        return ELF::R_ARM_MOVT_ABS;
       reportError(Fixup.getLoc(), "unsupported relocation type");
       return ELF::R_ARM_NONE;
     case FK_Data_4:
@@ -201,6 +207,10 @@ unsigned ARMELFObjectWriter::getRelocType(const MCFixup &Fixup,
   default:
     reportError(Fixup.getLoc(), "unsupported relocation type");
     return ELF::R_ARM_NONE;
+  case FK_Data_8:
+    // BOLT/ELF32 may emit 64-bit symbol diffs for padded functions. ARM has
+    // no R_ARM_ABS64; the addend/value still fits in 32 bits.
+    return ELF::R_ARM_ABS32;
   case FK_Data_1:
     switch (Specifier) {
     default:
@@ -278,6 +288,7 @@ unsigned ARMELFObjectWriter::getRelocType(const MCFixup &Fixup,
       reportError(Fixup.getLoc(), "invalid fixup for ARM MOVT instruction");
       return ELF::R_ARM_NONE;
     case ARM::S_None:
+    case ARM::S_HI16:
       return ELF::R_ARM_MOVT_ABS;
     case ARM::S_SBREL:
       return ELF::R_ARM_MOVT_BREL;
@@ -288,6 +299,7 @@ unsigned ARMELFObjectWriter::getRelocType(const MCFixup &Fixup,
       reportError(Fixup.getLoc(), "invalid fixup for ARM MOVW instruction");
       return ELF::R_ARM_NONE;
     case ARM::S_None:
+    case ARM::S_LO16:
       return ELF::R_ARM_MOVW_ABS_NC;
     case ARM::S_SBREL:
       return ELF::R_ARM_MOVW_BREL_NC;
